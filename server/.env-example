# Serveur
PORT=3000
APP_BASE_URL=http://localhost:5173
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/webdev-esilv

# JWT
JWT_SECRET=change_me_in_production_super_secret_key
JWT_COOKIE_NAME=auth_token

# Mailer (utiliser Ethereal pour les tests : https://ethereal.email)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your_ethereal_user
SMTP_PASS=your_ethereal_pass
SMTP_FROM=noreply@fashionstore.com

# Stripe (récupérer sur https://dashboard.stripe.com)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Crypto (wallet qui reçoit les paiements USDC)
MERCHANT_WALLET_ADDRESS=0xYourWalletAddressHere
